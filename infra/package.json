{
  "name": "@usd/infra",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "node -e \"console.log('@usd/infra: run pnpm --filter @usd/infra synth for CDK')\"",
    "build": "tsc",
    "watch": "tsc -w",
    "cdk": "cdk",
    "synth": "cdk synth",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "node -e \"process.exit(0)\""
  },
  "dependencies": {
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "aws-cdk": "^2.170.0",
    "esbuild": "^0.28.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}
